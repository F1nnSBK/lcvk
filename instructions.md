📋 Kopiere diesen Block für deinen KI-Agenten:PlaintextAct as a Senior Performance Engineer and Core SysML Researcher. We need to expand our benchmarking suite to map the exact performance crossover curve between Pithos Host-Native and Meta's FAISS Flat L2. We want to find the exact breaking points based on dimensionality and neighborhood size (K). Create a new benchmarking script `benchmark_sweep.py` and hook it into `run_benchmark.sh`. Enforce clean code standards, write all code/logs in English, and use minimal, high-impact comments. Execute the following implementation steps: 1.Implement the Dimensionality Sweep Loop:The script must systematically test the following vector dimensions: $D = [16, 32, 64, 128, 256, 384, 512, 768, 1024]$.For each dimension $D$:Generate a fixed database pool of 100,000 vectors and 278 queries using the previously implemented generate_hypersphere_vectors(num, dim) function (strictly L2-normalized).Run the Sequential JIT baseline, FAISS Flat L2, and Pithos Host-Native.Measure Latency (ms) and Throughput (MVPS) for each backend.2.Implement the Top-K Neighborhood Sweep Loop:To test the sorting and heap bottleneck, freeze the dimension at $D = 384$ and sweep through the following $K$ values: $K = [10, 50, 100, 250, 500, 1000]$.Measure how the Pithos priority queue scales against FAISS's native C++ heap selection structures.3.Detect and Log Crossover Points:The script must dynamically calculate the crossover point in the terminal output.Print an explicit message if FAISS outperforms Pithos at any stage (e.g., [CROSSOVER DETECTED] FAISS outperforms Pithos at D <= 32 due to low-dimensional float optimization floor).4.Telemetry Export & Matrix Generation:Save the sweep results into a structured JSON file named pithos_sweep_metrics.json. The structure must map:JSON{ "dimensionality_sweep": [ {"dim": 16, "pithos_mvps": 1200.0, "faiss_mvps": 1400.0, "winner": "faiss"}, {"dim": 384, "pithos_mvps": 1198.18, "faiss_mvps": 74.30, "winner": "pithos"} ], "top_k_sweep": [ {"k": 10, "pithos_ms": 105.0, "faiss_ms": 70.0}, {"k": 1000, "pithos_ms": 210.0, "faiss_ms": 95.0} ] } Ensure the script updates or generates a visual plotting asset (e.g., assets/crossover_curve.png) showing Throughput (Y-axis) vs. Dimensions (X-axis) for both backends.Wenn dein Agent das durchgezogen hat, liefert dir ein einziger Aufruf von ./run_benchmark.sh das vollständige mathematische Profil deines Systems. Du siehst schwarz auf weiß, bis zu welcher niedrigen Dimension FAISS gewinnt und ab welcher Stufe deine Bitmasken-Architektur die C++-Konkurrenz deklassiert.